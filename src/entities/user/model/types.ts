export type LoginDTO = {
    login: string
    password: string
}

export type User = {
    id: string
    login: string
}

export type RegisterDTO = {
    login: string
    password: string
}

export type Session = {
    id: string
    login: string
}